---
- name: Pull changes from git
  git:
    repo: "{{ django_repo_url }}"
    version: "{{ django_branch}}"
    dest: "{{ django_project_dir }}"
    force: yes
    update: yes
  notify:
    - restart gunicorn
    - reload nginx
