---
- name: Add kube-proxy confs
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: '0644'
  loop:
    - { src: kube-proxy-config.yaml.j2, dest: /var/lib/kube-proxy/ }
    - { src: kube-proxy.service.j2, dest: /etc/systemd/system/ }
  become: true