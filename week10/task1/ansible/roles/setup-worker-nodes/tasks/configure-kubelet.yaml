---
- name: Add kubelet confs
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    mode: '0644'
  loop:
    - { src: kubelet-config.yaml.j2, dest: /var/lib/kubelet/ }
    - { src: kubelet.service.j2, dest: /etc/systemd/system/ }
  become: true